import * as internal from "stream"

export interface ExpoItem{
  prod_name : string,
  quality : string,
  quantity : any,
  price : any,
  producing_country:string,
  goverment_agent:string,
  shippement_date:Date,
  state:string,
}
